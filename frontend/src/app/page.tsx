export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-950 px-6 text-zinc-50">
      <section className="w-full max-w-2xl text-center">
        <p className="text-sm font-medium uppercase tracking-[0.18em] text-teal-300">
          Clinic SaaS MVP
        </p>
        <h1 className="mt-4 text-4xl font-semibold sm:text-5xl">
          Project scaffold is ready.
        </h1>
        <p className="mt-6 text-base leading-7 text-zinc-300">
          Next.js and Tailwind CSS are configured. Domain features can be added when the product scope is ready.
        </p>
      </section>
    </main>
  );
}
